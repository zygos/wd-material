import { test } from '@playwright/test';

test.skip('navigating through days', async ({ page }) => {
  // exercise for E2E part:
  // 1. Focus on your test view in VS Code:
  // - Sidebar > Test icon or "Testing: Focus on Test Explorer View" in the command palette
  // 2. Make sure you have "Show browser" enabled.
  // 3. Create a new line below and click "Record at cursor".
  // 4. Go through what you want to test in the new browser window.
  // 5. Click "Cancel" when you're done.
  // 6. Wrap what you want to be as an assertion inside an await expect(...).toBeXXX() call.
});

test.skip('adding and removing APOD image in the homepage', async ({
  page,
}) => {});

test.skip('seeing favorite APODs in the favorites list', async ({
  page,
}) => {});

test.skip('persisting APOD images across page refreshes', async ({
  page,
}) => {});

// you can add additional tests
