import { it } from 'vitest'
import substituteVariables from '.'

// TODO: Remove the `.todo` methods and add tests for the `substituteVariables` function.
// Go through these tests one-by-one.
it.todo('returns the original string if no variables are provided')
it.todo('substitutes variables in a given string')
it.todo('allows substituting the same variable multiple times')
