import { it } from 'vitest'

it.todo('rejects passwords shorter than 9 characters')
it.todo('rejects passwords that do not contain a letter')
it.todo('rejects passwords that do not contain a number')
it.todo(
  'accepts passwords that are at least 9 characters long, contain a letter and a number'
)
